 public boolean isPowerOfTwo(int n) {
        if(n<0) return false;
        double power;
        for(int i=0;i<=30;i++)
        {   
         power=Math.pow(2,i);
        if(power==n)
        {
            return true;
        }
        }
        return false;
    }
